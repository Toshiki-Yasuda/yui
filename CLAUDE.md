# マリンバコンサート「結」公式サイト

## プロジェクト概要
マリンバコンサート「結」の公式ホームページ。静的HTML/CSS/JavaScriptサイトで、GitHub Pagesでホスティング。
- 公開URL: https://toshiki-yasuda.github.io/yui/
- 公演日: 2026年10月25日（日）13:30開演
- 会場: HITARU クリエイティブスタジオ（札幌市）

## 現在のフェーズ：2026年9月の全面リデザイン・運用
2026年9月12日、既存の文章・情報・水彩画・ブランドを保持してWeb版を全面リデザイン。
今回の公開対象・設計意図・検証範囲は [`docs/redesign-review.md`](docs/redesign-review.md) を最初に読むこと。
比較元コミットは `cd9b215`。以降の変更でも文章・公演情報・URLを勝手に書き換えない。

ユーザーから明示された最終仕様：
- YouTubeサムネイルは**右側に小さく控えめに**（PC 216px、600px以下140px）。再生時のみプレイヤーを拡大
- ページ最下部の「〜結〜」は**24pxで控えめに**。大きなロゴへ戻さない
- **OFUSEは見つけやすく**。独立した枠と明るいボタンを維持

進め方のルール：
- 運用時は1コミット = 1つの視覚的変更を基本にする。今回の全面リデザインはユーザー承認の一体的な変更
- 新しいCSS変数を増やす前に、既存トークンで組めないか必ず検討する
- 印刷用ページ（pamphlet / flyer）はリニューアル対象外。巻き込まない

## ファイル構成
- `index.html` — メインページ（公演情報・チケット・出演者紹介・FAQ等）
- `marimba.html` — マリンバ紹介ページ
- `pamphlet.html` — 印刷入稿用パンフレット（塗り足し対応）
- `flyer.html` — A4フライヤー（印刷用）
- `css/style.css` — 共通スタイルシート。`:root` のトークン定義が全デザインの単一の情報源
- `css/marimba.css` — 楽器紹介ページ専用スタイル
- `js/site.js` — 両ページ共通のナビ、スクロール演出、カウントダウン、動画、画像拡大
- `scripts/check-site.py` — 元の文章・リンク・構造化データ、ローカル参照、画像寸法等の検証
- `docs/redesign-review.md` — デザイン判断・実画面検証・未確認事項
- `favicon.svg` — ファビコン（「結」の文字）
- `images/` — 画像フォルダ

## 技術スタック
- HTML5 + CSS3 + Vanilla JavaScript（ビルドツール・外部JS依存なし）
- Google Fonts: Noto Sans JP / Noto Serif JP
- レスポンシブデザイン（モバイルファースト）
- CSS変数によるデザイントークン管理
- 構造化データ（JSON-LD: MusicEvent）

## デザイン規約
値は必ず `css/style.css` の `:root` トークンを使う。直書きの色・px・影は原則禁止。

- **色** — ピンク系（`--color-main`系）は装飾専用で、文字色とボタン背景には使わない。強調・CTA背景は `--color-rose-deep`。金で文字に使えるのは `--color-gold-deep` のみ、他は罫・縁・アイコン用。`--color-accent-light` も罫と境界線専用
- **幅** — 上記4段階のみ。5本目の基準線を作らない。`--w-reading`（640px＝日本語40〜45字）は行長の上限なので広げない
- **文字** — `--fs-2xs`〜`--fs-display` のスケールと `--fs-heading`、本文16px基準。未就学児から高齢の来場者までが読むため、下限を下げない
- **余白・角丸・影・モーション** — `--space-*` / `--radius-*` / `--shadow-*` / `--ease-*` を使う
- コントラストはAA（本文4.5:1）以上を維持する。使用箇所の実際の背景色との組み合わせで測定すること
- 和の要素と柔らかい雰囲気の両立、グラスモーフィズムの使用は継続

### 過去に踏んだ落とし穴
コードを読んでも気づきにくく、実際に表示が崩れた原因になったもの。

- **行長はコンテナ幅ではなく本文要素側で絞る** — セクションのコンテナ幅を主題ごとに変えると、
  見出しの罫線と左端が交互にズレて縦の基準線が消える。コンテナは共通のまま、
  本文の `p` などに `max-width: var(--w-reading)` を持たせる
- **グリッドアイテムに `margin-*: auto` を使うと `stretch` が外れて内容幅まで縮む** —
  デスクトップ2カラム用の中央寄せ指定が、1カラムに落ちたモバイルでブロックごと
  右へ押し出す原因になる。カラム数を変えるメディアクエリでは必ず打ち消す
- **サムネイルの左右をモバイルで逆転させない** — `.program-video-main` の画像は列2、説明は列1。
  再生後の `.is-playing` のときだけ1カラムに変更する
- **スクロール位置を保つ** — 固定ナビの高さ、`scroll-padding-top`、各セクションの
  `scroll-margin-top` を合わせている。余白を変えたらアンカー移動も確認する

## 実装の引き継ぎ
- ページ内に旧インラインJSや大量のfade-inはない。両ページとも `js/site.js` をdeferで読む
- FAQと出演者略歴はネイティブ `details/summary`。チラシはネイティブ `dialog`
- コンセプトの結び目をスクロールに連動して描く。ヒーロー・楽器絵の移動は控えめで600px以下では停止
- アニメーションのために本文を非表示にしない。`prefers-reduced-motion` では完成形を即表示
- スクロール処理はrequestAnimationFrameとIntersectionObserverで必要な対象だけ処理する。
  `pagehide` 時にイベント・監視・タイマーを解除し、bfcache復帰時に初期化する
- サムネイルは通常のYouTubeリンクでもある。JS有効時はクリック後にnocookie iframeを生成。
  JS無効時や修飾キー付きクリックでは通常のリンクとして機能する
- 外部YouTubeと地図は配信環境の影響を受けるため、外部リンクを消さない。
  ローカル検証ではYouTube実再生は未確認（詳細は検証記録）
- QR画像は同じチケットURLの `images/ticket-qr.png` をローカル配信する

## 確認方法
```bash
python3 -m http.server 8001 --bind 127.0.0.1
python3 scripts/check-site.py --baseline cd9b215
node --check js/site.js
git diff --check
```
- `http://127.0.0.1:8001/index.html` と `marimba.html` をブラウザで確認
- **モバイル・タブレット・デスクトップ・ワイド（1600px以上）の4幅を必ず見る**。
  目安は390×844、768×1024、1440×1000、1920×1080。320px幅も横はみ出しを確認する
- 実際のブラウザ画面で評価する。メニュー、FAQ、略歴、チラシ拡大、動画、リンク、キーボード操作も確認
- モーション低減とJSなしのフォールバックを維持する。旧実装のfade-inを可視化するための上書きは不要
- 大きすぎるviewport高さはsvhによる実際のレイアウトを変える。通常の高さでスクロールして確認する
- 一時検証ファイルを作った場合は公開・コミット前に削除する

## モデルとエージェントの使い分け
コンテキストとコストの効率を優先し、思考の重さでモデルを振り分ける。

- **Opus（メインセッション）** — デザイン判断、構成・情報設計の検討、崩れの原因調査、計画立案、レビュー指摘の取捨選択。「どうするか」を決める作業はここから出さない
- **Sonnetへ委譲** — 仕様が確定した実装、複数箇所の一括置換、既存パターンに沿ったマークアップ追加、リファクタ
- **Haikuへ委譲** — 機械的な確認作業。grepでの洗い出し、リンク切れ・alt属性・見出し階層のチェック、スクリーンショット撮影と報告

委譲のルール：
- 渡すときは「対象ファイル／確定した仕様／触ってはいけない箇所」を明記する。判断の余地が残るタスクは委譲しない
- 委譲先の成果は鵜呑みにせず、Opus側で結果を確認してからコミットする
- worktreeでの並列化は、3ファイル以上を独立に触るときだけ。単発の変更では使わない

## デプロイ
GitHub Pagesの設定は `main` ブランチのルート `/`（legacy branch build）。
ビルドやnpm installは不要。ユーザーが公開を依頼したとき、必要な変更をコミットして通常のpushを行う。
今回（2026年9月12日）はユーザーから公開の明示的な依頼あり。将来の編集も自動的に公開承認されたと解釈しない。

1. `git status --short` と差分を確認し、一時ファイル・秘密情報を含めない
2. 上記の自動検証と実画面確認を実施
3. `git fetch origin` でmainの変更を確認。競合は解決し、force pushしない
4. 必要ファイルだけをstage・commitし、`git push origin main`
5. `gh api repos/Toshiki-Yasuda/yui/pages/builds/latest` で `status: built` と対象コミットを確認
6. 公開URLをブラウザで開き、実際の表示と新しいCSS/JS・画像の配信を確認

リモート: `https://github.com/Toshiki-Yasuda/yui`

公開URL: `https://toshiki-yasuda.github.io/yui/`

push成功だけでは公開完了と判断しない。Pagesのビルド完了と公開URLの反映まで確認する。
ロールバックが必要な場合は対象コミットをrevertする通常のコミットで戻す（履歴を破壊しない）。

## 注意事項
- 印刷用ページ（pamphlet.html, flyer.html）は塗り足し・トンボなどの印刷仕様あり。編集時は印刷要件を崩さないこと
- コンテンツは日本語。コミットメッセージも日本語プレフィックス（feat/fix/update/add/style/remove）で統一
